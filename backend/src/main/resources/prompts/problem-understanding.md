Read this problem the way a strong competitor would on the first pass.

# Problem
Title: {{problemTitle}}
Difficulty (if known): {{difficulty}}
Topics (if known): {{topics}}

{{problemStatement}}

Stated constraints:
{{constraints}}

# Task
Work out what is actually being asked, what is easy to misread, and what the constraint magnitudes
imply about the intended complexity.

Return JSON:
{
  "restatement": "the problem in two plain sentences, no jargon",
  "inputs": ["each input and what it means"],
  "output": "exactly what must be returned, including type and indexing base",
  "constraints": ["each constraint, normalised"],
  "easyToMisread": ["specific traps in the wording that change the algorithm"],
  "topics": ["canonical topic tags"],
  "difficultyGuess": "EASY | EASY_MEDIUM | MEDIUM | MEDIUM_HARD | HARD",
  "clarifyingQuestions": ["questions the learner should answer before writing code"]
}
