# Base URL the browser uses to reach the API.
# Leave as-is for local development: Next proxies /backend/* to the Spring Boot service so there is
# no CORS configuration to get wrong.
NEXT_PUBLIC_API_BASE_URL=/backend

# Where the Next server forwards those proxied requests. In Docker Compose this is the service name.
BACKEND_INTERNAL_URL=http://localhost:8080
